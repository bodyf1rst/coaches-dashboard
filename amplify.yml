version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --legacy-peer-deps
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/BodyF1rst-Web-1.0
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*